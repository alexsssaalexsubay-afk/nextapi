{
  "name": "nextapi",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "cd backend && go test -race -count=1 ./...",
    "typecheck": "pnpm -r typecheck",
    "director:smoke": "node scripts/smoke-director-closed-loop.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
