# Project AGENTS

## Mission
Minimal, reviewable `nextapi-v3` slices. Working vertical > broad scaffolding.

## Rules
1. Read the nearest design doc or write a focused module doc first.
2. Implement the smallest useful slice, then validate (build/test/run).
3. `STATUS.md` only marks "closed-loop" with executable evidence.
4. Do not expand scope while current slice is unvalidated.
5. Keep names aligned: routes, env vars, docs, code.
6. MCP disabled in this workspace unless user asks.
